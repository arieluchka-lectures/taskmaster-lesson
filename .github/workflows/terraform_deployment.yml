name: Terraform deployment

on:
  push:
    branches:
      - main

jobs:
  hello_world:
    name: My first job
    runs-on: ubuntu-latest

    steps:
      - name: Check code
        uses: actions/checkout@v4

      - name: first job
        run: |
          ls -al
          echo "#######"
          pwd
          echo hello maryam